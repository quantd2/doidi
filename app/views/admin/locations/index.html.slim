h3 All locations
= nested_resources @locations.arrange(:order => :created_at)
/ ol.categories-list
/   table class="table table-striped"
/     thead
/       tr
/         th Name
/         th Action
/     tbody
/     - @categories.each do |category|
/       tr
/         td = category.name
/         td = link_to "Delete", admin_category_path(category), method: :delete, data: { confirm: "Bạn có muốn xoá '#{category.name}' không?" }

= link_to 'New location', new_admin_location_path
= paginate @locations
